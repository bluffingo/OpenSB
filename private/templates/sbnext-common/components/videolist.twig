{% for video in videos %}
    {{ small_video_box(video) }}
{% endfor %}
<div class="center">
    <div class="btn btn-default" onclick="showMoreVideos()" id="fromUser">
        {{ __("More") }}
    </div>
</div>