<div class="grid commentPost">
    <div class="col-md-3">
        {{ userlink(data, 'u_') }}
        {% if data.u_powerlevel > 1 %}{{ icon("shield", 16) }}{% endif %}
		<br>
		<span class="time">{{ data.date | relative_time }}</span>
    </div>
    <div class="col-md-13" id="{{ data.comment_id }}">
        <div class="comment">
            {{ data.comment | markdown }}
        </div>
        <br>
        <p>
            {% if data.replycount > 0 %}
                <a onclick="showReplies({{ data.comment_id }})">Show replies</a> |
            {% endif %}
            {% if data.id|length > 0 %}
                <a onclick="reply({{ data.comment_id }})">Reply</a>
            {% endif %}
        </p>
    </div>
</div>
