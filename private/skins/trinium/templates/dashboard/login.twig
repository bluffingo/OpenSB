{% extends "_layout_login.twig" %}

{% set title = "Dashboard login" %}

	{% block content %}
        <div class="section">
            <div class="section-header">
                <h1 class="section-title">Dashboard login</h1>
            </div>
            <div class="content-box">
                <div class="login-currently-logged-in">
                    <div class="login-pfp">
                        <div class="profile-picture">
                            <img src="{{ profile_picture(user_data.id) }}" class="pfp"
                                 alt="{{ user_data.name }}">
                        </div>
                    </div>
                    <div>
                        <div class="login-currently-logged-in-text">
                            You are currently logged in as {{ user_data.name }}.
                        </div>
                    </div>
                </div>
                <hr>
                <div class="panel-body">
                    <form method="post" name="loginForm" id="loginForm" action="/dashboard/login" class="form-login">
                        <input type="hidden" name="csrf_token" value="{{ session.csrf_token }}">
                        <div class="form-input">
                            <label for="password">Dashboard access password</label>
                            <input type="password" name="password">
                        </div>

                        {% if is_chaziz_sb %}
                            <p class="text-danger">
                                <strong>IMPORTANT</strong>: Your dashboard access password is different from your normal password.
                                If you lose it, please contact Chaziz in private.
                            </p>
                        {% else %}
                            <p class="text-danger">
                                <strong>IMPORTANT</strong>: Your dashboard access password is different from your normal password.
                                If you lose it, please contact the instance owner in private.
                            </p>
                        {% endif %}

                        <div class="login-bottom">
                            <div class="form-button-container">
                                <input type="submit" class="button accent button-login" value="{{ localize('login') }}" name="loginsubmit">
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    {% endblock %}