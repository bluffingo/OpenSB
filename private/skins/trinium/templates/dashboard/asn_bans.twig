{% extends "_layout_dashboard.twig" %}

{% if search %}
    {% set le_url = '?page=%s&amount=' ~ amount  ~ "&search=" ~ search %}
{% else %}
    {% set le_url = '?page=%s&amount=' ~ amount %}
{% endif %}

{% set title = "ASN bans" %}

{% set breadcrumbs = [
    { url: '/dashboard/asn_bans', label: "ASN bans" },
] %}

{% block dashboard_content %}
    {% set total_pages = (data.count / amount) | round(0, 'ceil') %}
    {% set current_page = page %}

    <div class="content-box">
        <div style="text-align:center;">
            <form method="POST" action="/dashboard/asn_bans/">
                <input type="text" name="asn" placeholder="ASN">
                <button type="submit" class="button danger">Ban</button>
            </form>
        </div>
        <hr>
        <div class="form-flex">
            <form action="/dashboard/asn_bans/" method="GET" id="num_change" class="admin-form-select">
                <select name="amount" onchange="this.form.submit()">
                    {% for option in [8, 16, 24, 32, 48, 64, 96, 128, 256] %}
                        <option value="{{ option }}" {{ amount == option ? 'selected' }}>{{ option }}</option>
                    {% endfor %}
                </select>
            </form>

            <form method="GET" asction="/dashboard/asn_bans/" class="admin-form-search">
                <input type="hidden" name="amount" value="{{ amount }}">
                <input type="text" name="search" value="{{ search }}" placeholder="{{ localize('search') }}...">
                <button type="submit" class="button secondary">
                    {{ icon('search') }}
                </button>
            </form>
        </div>
        <hr>
        {% for asn in asns %}
            <div class="content-entry">
                <div class="content-info">
                    <div class="content-title">
                        {{ asn.asn }}
                    </div>
                    <div class="content-detail">By: {{ asn.author }}</div>
                    <div class="content-detail">Time: {{ asn.timestamp | format_date('long', 'medium') }} ({{ asn.timestamp | relative_time() }})</div>
                </div>
            </div>
        {% endfor %}
        {{ pagination(count, amount, le_url, current_page) }}
    </div>
{% endblock %}