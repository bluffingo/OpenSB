<div class="grid">
    <div class="col-4">
        <div class="section">
            <div class="content-box highlight-box no-border-bottom-rounded">
                <div class="section-header flex">
                    <div class="section-header-left">
                        <h2 class="section-title">{{ common.displayname }}</h2>
                    </div>
                    <div class="section-header-right">
                        {% if logged_in %}
                            {% if not common.is_current %}
                                <button class="button secondary small" id="follow-user">
                                    {% if common.following %}
                                        {{ localize('unfollow') }}
                                    {% else %}
                                        {{ localize('follow') }}
                                    {% endif %}
                                </button>
                            {% else %}
                                {% if enable_incomplete_features and options.exp_trinium_profile_settings %}
                                    <button class="button secondary small" id="edit-profile">{{ localize('edit') }}</button>
                                {% else %}
                                    <a href="/my_profile">{{ localize('edit') }}</a>
                                {% endif %}
                            {% endif %}
                        {% endif %}
                    </div>
                </div>
                <div class="profile-box">
                    <div class="profile-box-pfp">
                        <div class="profile-picture">
                            <img src="{{ profile_picture(common.id) }}" class="pfp" alt="{{ common.username }}">
                        </div>
                    </div>
                    <div class="profile-box-info">
                        <div class="profile-box-username">
                            <span>@{{ common.username }}</span>
                            {% if common.is_staff %}
                                <span class="profile-box-icon use-tooltip" title="{{ localize('staff') }}">{{ icon('staff') }}</span>
                            {% endif %}
                        </div>
                        <div class="profile-box-details">
                            <span id="follower_count">{{ common.followers }}</span> followers
                        </div>
                    </div>
                    {#<div class="profile-box-follow">
                    </div>#}
                </div>
            </div>
            <div class="content-box highlight-box no-border-top">
                {% if common.about | trim is not empty %}
                    <div class="profile-about">
                        {{ common.about | markdown_user_written }}
                        <hr>
                    </div>
                {% endif %}
                <div class="profile-details">
                    <ul class="profile-details-list">
                        <li class="detail-item">
                            <span class="detail-label">{{ localize('registered') }}:</span>
                            <span class="detail-value">{{ common.joined | format_date('long', 'none') }}</span>
                        </li>
                        {% if common.last_active %}
                            <li class="detail-item">
                                <span class="detail-label">{{ localize('last_active') }}:</span>
                                <span class="detail-value">{{ common.last_active | relative_time() }}</span>
                            </li>
                        {% endif %}
                    </ul>
                    {% if user_is_authenticated_staff %}
                        <hr>
                        <a href="/dashboard/users/{{ common.username }}">{{ localize('dashboard_lookup') }}</a>
                    {% endif %}
                </div>
            </div>
        </div>

        <div class="section">
            {% if data.journals %}
            <div class="content-box basic-box">
                <div class="section-header flex">
                    <div class="section-header-left">
                        <h2 class="section-title">{{ localize('journals') }}</h2>
                    </div>
                    <div class="section-header-right">
                        <a href="/user/{{ common.username }}/journals">{{ localize('more') }}</a>
                    </div>
                </div>
                    {% for journal in data.journals %}
                        {{ include('/components/_journal.twig') }}
                    {% endfor %}
            </div>
            {% endif %}
        </div>
    </div>
    <div class="col-8">
        {% if data.featured_upload %}
        <div class="section">
            <div class="content-box basic-box">
                <div class="section-header">
                    <h2 class="section-title">{{ localize('profile_featured') }}</h2>
                </div>
                {% include('/components/_upload_vertical.twig') with {'upload': data.featured_upload} %}
            </div>
        </div>
        {% endif %}

        {% if data.uploads %}
            <div class="section">
                <div class="content-box basic-box">
                    <div class="section-header flex">
                        <div class="section-header-left">
                            <h2 class="section-title">{{ localize('uploads') }}</h2>
                        </div>
                        <div class="section-header-right">
                            <a href="/user/{{ common.username }}/uploads">{{ localize('more') }}</a>
                        </div>
                    </div>
                    <div class="content-grid">
                        {% for upload in data.uploads %}
                            {{ include('/components/_upload_grid.twig') }}
                        {% endfor %}
                    </div>
                </div>
            </div>
        {% else %}
            <div class="section">
                <div class="content-box basic-box">
                    <div class="display-message">{{ common.username }} has not uploaded anything.</div>
                </div>
            </div>
        {% endif %}

        {% if enable_incomplete_features and options.exp_collections %}
        <div class="section">
            <div class="content-box basic-box">
                <div class="section-header">
                    <h2 class="section-title">Collections</h2>
                </div>
                <div>
                    PLACEHOLDER
                </div>
            </div>
        </div>
        {% endif %}

        <div class="section">
            <div class="content-box basic-box">
                <div class="section-header flex">
                    <div class="section-header-left">
                        <h2 class="section-title">{{ localize('comments') }}</h2>
                    </div>
                    <div class="section-header-right">
                        <a href="/user/{{ common.username }}/comments">
                            {{ localize('more') }}
                        </a>
                    </div>
                </div>
                {{ include('/components/_comment_input.twig') }}
                {% for comment in data.comments %}
                    {{ include('/components/_comment.twig') }}
                {% endfor %}
            </div>
        </div>
    </div>
</div>