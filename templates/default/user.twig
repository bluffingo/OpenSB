{% extends "_layout.twig" %}

{% block title %}{{ user.name }}{% endblock %}

	{% block head %}
		<script>
			user_id = '{{ user.id }}';
		</script>
	{% endblock %}

	{% block precontent %}
<style>
	{{ profCss | raw }}
	.bg-channel-background {
	background-image: url('{{ channel_background(user.name) }}');
    margin-top: -24px;
	padding-bottom: 25px;
	}
</style>
<div class="bg-channel-background">
	<div class="col-12 mb-2 mt-0">
	<div class="p-1 bg-primary text-white">
		<div class="container py-4">
			<div class="row">
				<div class="col-md-10">
					<img class="float-start rounded-circle me-3" src="{{ profile_image(user.name) }}" width="150" alt="pfp">
					<h1 class="align-top fw-bold mb-0">
						{{ user.title }}{% if user.u_flags == 1 %}<svg data-bs-toggle="tooltip"
							data-bs-placement="top" title="{{ __("Checkmarked") }}"
							style="margin-left: 0.75%;" class="bi" width="32" height="32" fill="currentColor">
							<use xlink:href="vendor/twbs/bootstrap-icons/bootstrap-icons.svg#patch-check-fill" />
						</svg>{% endif %}</h1>
						<div class="fs-4">@{{ user.name }}</div>
					<p class="fs-6 text-break">{{ user.about }}</p>
				</div>
				<div class="col-md-2">
					<button id="subscribe" class="btn btn-{% if subscribed %}secondary{% else %}warning{% endif %}" type="button" {% if not log or userdata.id == user.id %}disabled{% endif %}>{% if subscribed %}{{ __("Unsubscribe") }}{% else %}{{ __("Subscribe") }}{% endif %}</button>
				</div>
			</div>
		</div>
	</div>
</div>
	{% endblock %}

	{% block content %}
<div class="row">
	{% if edited %}
	<div class="col-12">
		<div class="alert alert-success alert-dismissible fade show" role="alert">
			{{ __("Your profile has been successfully updated.") }}
			<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
		</div>
	</div>
	{% endif %}
	<!-- This prevents the user info box from being extremely tall. Box probably needs to be redesigned... -->
	<div class="col-md-8 order-2 order-md-1">
		<div class="card">
			<div class="card-body">
				<div class="caption">
					<h3>{{ __("Uploaded by %s", [user.title]) }}</h3>
					{% for latestVideo in latestVideos %}
					{{ video_box(latestVideo) }}
					{% else %}
					<p>{{ __("There are currently no videos uploaded by %s.", [user.title]) }}</p>
					{% endfor %}
				</div>
			</div>
		</div>
	</div>
	<div class="col-md-4 order-1 order-md-2">
		<div class="card mb-2">
			<div class="card-body">
				<h4>{{ __("About %s", [user.title]) }}</h4>
				<hr>
				<dl class="row mb-0">
					<dt class="col-auto">{{ __("Last seen") }}</dt>
					<dd class="col-auto">{{ user.lastview | relative_time }}</dd>
				</dl>
				<dl class="row mb-0">
					<dt class="col-auto">{{ __("Joined") }}</dt>
					<dd class="col-auto">{{ user.joined | date('M j, Y') }}</dd>
				</dl>
				<dl class="row mb-0">
					{# hack to prevent twig from outputing the number 1 #}
					{% if subCount != 1 %}
						{% set pluralSubscribers = __("s") %}
					{% endif %}
					<dt class="col-auto">{{ __("Subscriber%s", [pluralSubscribers]) }}</dt>
					<dd class="col-auto">{{ subCount }}</dd>
				</dl>
				</div>
			</div>
			{% if userdata.powerlevel > 2 %}
			<div class="card mb-2">
				<div class="card-body">
					<h4>{{ __("Administrative Actions") }}</h4>
					<hr>
					<dl class="row">
						<a href="resetpassword.php?grf={{ user.id }}">{{ __("Generate password reset for %s", [user.name]) }}</a>
					</dl>
				</div>
			</div>
			{% endif %}
		</div>
	</div>
</div>
</div>
<div class="container">
	{% endblock %}
