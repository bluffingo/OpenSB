{% extends "_layout.twig" %}

{% block title %}{{ __("Home") }}{% endblock %}

	{% block content %}
        <div>
            {% for video in videos %}
                {{ vertical_video_box(video) }}
            {% else %}
                <p>{{ __("You do not have any uploaded videos!") }}</p>
            {% endfor %}
        </div>

    {% endblock %}
